- name: Create directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - /etc/cni/net.d
    - /opt/cni/bin
    - /var/lib/kube-proxy
    - /var/lib/kubelet
    - /var/lib/kubernetes
    - /var/run/kubernetes
